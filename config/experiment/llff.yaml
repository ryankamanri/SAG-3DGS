# @package _global_

defaults:
  - override /dataset: re10k
  - override /dataset/view_sampler: mvsnerf
  - override /model/encoder: cascade
  - override /loss: [mse, lpips, depth, struct, color]

wandb:
  name: llff
  tags: [llff, 640x960]

data_loader:
  train:
    batch_size: 1

optimizer:
  max_steps: 300_001

trainer:
  max_steps: 300_001

# ----- Additional params for default best model customization
model:
  encoder:
    num_depth_candidates: 128
    costvolume_unet_feat_dim: 128
    costvolume_unet_channel_mult: [1,1,1]
    costvolume_unet_attn_res: [4]
    gaussians_per_pixel: 1
    depth_unet_feat_dim: 32
    depth_unet_attn_res: [16]
    depth_unet_channel_mult: [1,1,1,1,1]
    voxel_size_list: [16, 32, 64, 128, 256]
    patch_size_list: [25, 16, 9, 4, 1]


dataset: 
  name: llff
  image_shape: [640, 960] # 
  roots: [datasets/llff]
  baseline_scale_bounds: false
  make_baseline_1: false
  skip_bad_shape: false

test:
  compute_scores: true
  dataset_verify_path: datasets/origin/nerf_llff_data
